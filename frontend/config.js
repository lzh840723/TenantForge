// Optional runtime configuration for the demo frontend.
// Set your deployed backend base URL here to make it the default for all visitors.
// Example: window.BACKEND_BASE_URL = 'https://your-domain.up.railway.app';
// If unset, the app falls back to: URL query ?base=..., meta[name=backend-base-url], or http://localhost:8080.
window.BACKEND_BASE_URL = 'https://tenantforge-production.up.railway.app';
